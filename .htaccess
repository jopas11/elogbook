RewriteEngine On

# Route image requests through proxy (server root != app root)
RewriteRule ^public/uploads/(.*)$ image_proxy.php?path=$1 [L,QSA]

RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php?url=$1 [QSA,L]
